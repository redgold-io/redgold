CREATE TABLE IF NOT EXISTS test_table (
                                    hash BLOB PRIMARY KEY,
                                    height INTEGER,
                                    raw BLOB,
                                    time INTEGER
);
